name: Org Checkov
on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_call:

jobs:
  Checkov_Scan:
    uses: Coalfire-CF/Actions/.github/workflows/org-checkov.yml@main